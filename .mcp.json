{
  "mcpServers": {
    "brain": {
      "type": "http",
      "url": "${user_config.endpoint}/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.api_key}"
      }
    }
  }
}
